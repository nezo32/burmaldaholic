package dev.nezo.burmaldaholic.core.pvp.logic;

/** Where a match is anchored (PVP.md §3.1): a machine block, or none (host position / duel challenger). */
public enum AnchorKind {
	NONE, SLOT_MACHINE, WHEEL_OF_FORTUNE, PLINKO_MACHINE
}
