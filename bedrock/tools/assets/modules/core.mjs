// Core presentation art (global.md §5): panels, buttons, HUD, fx sprites, glyph sheet E1, particles.
// SKELETON: returns nothing yet (lane X-L0 fills it; gen-glyphs.mjs / gen-textures.mjs fold in here).
/** @returns {Array<{edition:string,path:string,bytes:Buffer}>} */
export default function generate() {
  return [];
}
